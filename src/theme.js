import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#470651',
    },
    secondary: {
      main: '#ECEBE9',
    },
    background: {
      default: '#ECEBE9',
      paper: '#FFFFFF',
    },
    text: {
      primary: '#470651',
    },
  },
});

export default theme;
